<?xml version="1.0" encoding="utf-8"?>
<!-- Mientras se mantiene pulsado. Sube el rojo para que se vea que está
     cargando, sin llegar todavía al rojo de alarma. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient android:angle="270" android:startColor="#3A171A" android:endColor="#241012" />
            <stroke android:width="1dp" android:color="@color/rd" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    <item android:gravity="top|end" android:top="-22dp" android:end="-22dp"
        android:width="44dp" android:height="44dp">
        <rotate android:fromDegrees="45" android:toDegrees="45"
            android:pivotX="50%" android:pivotY="50%">
            <shape android:shape="rectangle">
                <solid android:color="@color/bg" />
            </shape>
        </rotate>
    </item>
</layer-list>
